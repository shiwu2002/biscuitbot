# list_exec_sessions

列出当前所有执行会话。

## 何时使用

需要查看正在运行的进程会话、获取 session_id 或监控执行状态时使用。配合 write_stdin 使用，适合管理异步执行的任务。

## 参数

无参数。

## 调用示例

```
list_exec_sessions()
```

## 注意事项

- 返回所有活动会话及其状态信息
- 会话 ID 可用于 write_stdin 写入输入
- 已结束的会话可能不在列表中
- 会话过多时结果可能较长
